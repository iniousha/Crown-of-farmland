package edu.kit.kastel.model;

/**
 * this enum represents the two modes in which a board can be displayed.
 * @author ucktt
 */
public enum Verbosity {

    /**
     * displays the board using all the available symbols.
     */
    ALL,

    /**
     * displays the board with compact symbol representation.
     */
    COMPACT
}
